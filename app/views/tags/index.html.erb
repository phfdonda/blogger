<h1>All tags</h1>
<ul id="tags">
<% @tags.each do |tag| %>
<li>
  <%= link_to tag.name, tag_path(tag), class: "tag_name" %>
  <%= link_to "Delete tag", tag, method: :delete, data: {confirm: "Are you sure you want to delete this tag?"} %>
</li>
<% end %>
</ul>